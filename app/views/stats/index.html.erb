<div class="min-h-screen lemonade-background p-4">
  <div class="container mx-auto">
    <!-- ヘッダー -->
    <div class="neuro-card p-6 mb-6">
      <div class="flex items-center justify-start">
        <%= link_to diaries_path, class: "neuro-button p-3 mr-4 inline-flex items-center justify-center" do %>
          <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
          </svg>
        <% end %>

        <h1 class="text-3xl font-bold text-base-content">統計ダッシュボード 📊</h1>
      </div>
    </div>

    <!-- メインチャートグリッド -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
      
      <!-- 日毎推移チャート（大きめ） -->
      <div class="lg:col-span-2 neuro-card p-6">
        <div data-controller="chart-switcher"
             data-chart-switcher-chart-url-value="/stats/chart_data"
             data-chart-switcher-current-view-type-value="<%= @view_type %>"
             data-chart-switcher-current-month-value="<%= @target_month %>">
          
          <!-- チャートヘッダー -->
          <div class="flex items-center justify-between mb-6">
            <h2 class="flex items-center gap-2 text-2xl font-bold text-primary">
              <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
              </svg>
              日毎の推移
            </h2>
            
            <!-- 表示切り替えコントロール -->
            <div class="flex items-center gap-4">
              <select data-chart-switcher-target="viewSelect" 
                      data-action="change->chart-switcher#switchView"
                      class="neuro-input px-3 py-2 text-sm">
                <option value="recent" <%= 'selected' if @view_type == 'recent' %>>直近30日</option>
                <option value="monthly" <%= 'selected' if @view_type == 'monthly' %>>月ごと</option>
              </select>
              
              <!-- 月選択コントロール -->
              <div data-chart-switcher-target="monthControls" 
                   class="flex items-center gap-2 <%= 'hidden' if @view_type == 'recent' %>">
                <button type="button" 
                        data-action="click->chart-switcher#previousMonth"
                        class="neuro-button px-3 py-2 text-sm">
                  ←
                </button>
                
                <input type="month" 
                       data-chart-switcher-target="monthSelect"
                       data-action="change->chart-switcher#changeMonth"
                       value="<%= @target_month %>"
                       class="neuro-input px-3 py-2 text-sm">
                
                <button type="button" 
                        data-action="click->chart-switcher#nextMonth"
                        class="neuro-button px-3 py-2 text-sm">
                  →
                </button>
              </div>
            </div>
          </div>
          
          <!-- チャートコンテナ -->
          <div data-chart-switcher-target="chartContainer" class="neuro-input p-6 w-full">
            <div data-controller="chart" 
                 data-chart-type-value="<%= @daily_trends_chart[:type] %>"
                 data-chart-data-value="<%= @daily_trends_chart[:data].to_json %>"
                 data-chart-options-value="<%= @daily_trends_chart[:options].to_json %>">
              <canvas class="w-full" style="height: 400px;"></canvas>
            </div>
          </div>
          
          <div class="mt-4">
            <div class="neuro-badge p-4 text-sm text-base-content/70">
              📈 記録した日のみを表示し、線でつないで推移を確認。継続記録でパターンを把握しましょう。
            </div>
          </div>
        </div>
      </div>

      <!-- 学習強度ヒートマップ -->
      <div class="neuro-card p-6">
        <h2 class="flex items-center gap-2 text-xl font-bold text-primary mb-6">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v4a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
          学習強度ヒートマップ
        </h2>
        
        <div class="neuro-input p-4 w-full">
          <div data-controller="heatmap" 
               data-heatmap-data-value="<%= @learning_intensity_heatmap[:data].to_json %>"
               data-heatmap-options-value="<%= @learning_intensity_heatmap[:options].to_json %>">
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-3 text-sm text-base-content/70">
            📊 直近90日間の学習強度をGitHub風ヒートマップで表示。濃い色ほど高強度です。
          </div>
        </div>
      </div>


      <!-- 曜日別パフォーマンスパターン -->
      <div class="neuro-card p-6">
        <h2 class="flex items-center gap-2 text-xl font-bold text-primary mb-6">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
          曜日別パターン
        </h2>
        
        <div class="neuro-input p-4 w-full">
          <div data-controller="weekday-chart" 
               data-weekday-chart-type-value="<%= @weekday_pattern_chart[:type] %>"
               data-weekday-chart-data-value="<%= @weekday_pattern_chart[:data].to_json %>"
               data-weekday-chart-options-value="<%= @weekday_pattern_chart[:options].to_json %>">
            <canvas class="w-full" style="height: 300px;"></canvas>
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-3 text-sm text-base-content/70">
            📅 曜日ごとの平均パフォーマンスで生活リズムを把握
          </div>
        </div>
      </div>

      <!-- レベル分布チャート -->
      <div class="neuro-card p-6">
        <h2 class="flex items-center gap-2 text-xl font-bold text-primary mb-6">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z"></path>
          </svg>
          各項目のレベル分布
        </h2>
        
        <div class="neuro-input p-4 w-full">
          <div data-controller="chart" 
               data-chart-type-value="<%= @distribution_chart[:type] %>"
               data-chart-data-value="<%= @distribution_chart[:data].to_json %>"
               data-chart-options-value="<%= @distribution_chart[:options].to_json %>">
            <canvas class="w-full" style="height: 300px;"></canvas>
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-3 text-sm text-base-content/70">
            📊 レベル1〜5の出現頻度を確認。どの状態が多いか把握できます。
          </div>
        </div>
      </div>

            <!-- 月別投稿数チャート -->
      <div class="neuro-card p-6">
        <h2 class="flex items-center gap-2 text-xl font-bold text-primary mb-6">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v4a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
          月別投稿数
        </h2>
        
        <div class="neuro-input p-4 w-full">
          <div data-controller="chart" 
               data-chart-type-value="<%= @monthly_posts_chart[:type] %>"
               data-chart-data-value="<%= @monthly_posts_chart[:data].to_json %>"
               data-chart-options-value="<%= @monthly_posts_chart[:options].to_json %>">
            <canvas class="w-full" style="height: 300px;"></canvas>
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-3 text-sm text-base-content/70">
            📅 月ごとの日記投稿頻度を確認
          </div>
        </div>
      </div>

    </div>

    <!-- フッターヒント -->
    <div class="mt-8 mb-10 neuro-card p-6">
      <div class="text-center">
        <h3 class="text-lg font-bold text-primary mb-4">🌱 統計活用のヒント</h3>
        <div class="grid md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-3 text-sm text-base-content/70">
          <div class="neuro-badge p-3">
            <strong>📈 日毎推移</strong><br>
            記録した日を線で繋いで短期・月単位の変化を把握
          </div>
          <div class="neuro-badge p-3">
            <strong>📊 強度ヒートマップ</strong><br>
            90日間の学習強度をGitHub風マップで一覧表示
          </div>
          <div class="neuro-badge p-3">
            <strong>📅 月次カレンダー</strong><br>
            今月の記録状況を美しいカレンダー形式で確認
          </div>
          <div class="neuro-badge p-3">
            <strong>📅 曜日パターン</strong><br>
            曜日別の平均値で生活リズムを最適化
          </div>
          <div class="neuro-badge p-3">
            <strong>📊 月別投稿</strong><br>
            継続習慣の維持状況を月ごとの投稿数で確認
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
