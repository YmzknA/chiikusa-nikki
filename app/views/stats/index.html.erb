<div class="min-h-screen lemonade-background p-4">
  <div class="container mx-auto">
    <!-- ヘッダー -->
    <div class="neuro-card p-6 mb-6">
      <div class="flex items-center justify-between">
        <h1 class="text-3xl font-bold text-base-content">統計ダッシュボード 📊</h1>
        <%= link_to root_path, class: "neuro-button py-3 px-6 font-medium flex items-center gap-2" do %>
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          ホームに戻る
        <% end %>
      </div>
    </div>

    <!-- メインチャートグリッド -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
      
      <!-- 日毎推移チャート（大きめ） -->
      <div class="lg:col-span-2 neuro-card p-6">
        <h2 class="flex items-center gap-2 text-2xl font-bold text-primary mb-6">
          <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
          </svg>
          日毎の推移
        </h2>
        
        <div class="neuro-input p-6 w-full">
          <div data-controller="chart" 
               data-chart-type-value="<%= @daily_trends_chart[:type] %>"
               data-chart-data-value="<%= @daily_trends_chart[:data].to_json %>"
               data-chart-options-value="<%= @daily_trends_chart[:options].to_json %>">
            <canvas class="w-full" style="height: 400px;"></canvas>
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-4 text-sm text-base-content/70">
            📈 記録した日のみを表示し、線でつないで推移を確認。継続記録でパターンを把握しましょう。
          </div>
        </div>
      </div>

      <!-- 月別投稿数チャート -->
      <div class="neuro-card p-6">
        <h2 class="flex items-center gap-2 text-xl font-bold text-primary mb-6">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v4a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
          月別投稿数
        </h2>
        
        <div class="neuro-input p-4 w-full">
          <div data-controller="chart" 
               data-chart-type-value="<%= @monthly_posts_chart[:type] %>"
               data-chart-data-value="<%= @monthly_posts_chart[:data].to_json %>"
               data-chart-options-value="<%= @monthly_posts_chart[:options].to_json %>">
            <canvas class="w-full" style="height: 300px;"></canvas>
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-3 text-sm text-base-content/70">
            📅 月ごとの日記投稿頻度を確認
          </div>
        </div>
      </div>

      <!-- 今日の状態サマリー -->
      <div class="neuro-card p-6">
        <h2 class="flex items-center gap-2 text-xl font-bold text-primary mb-6">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
          </svg>
          今日の状態
        </h2>
        
        <div class="neuro-input p-4 w-full">
          <div data-controller="chart" 
               data-chart-type-value="<%= @today_summary_chart[:type] %>"
               data-chart-data-value="<%= @today_summary_chart[:data].to_json %>"
               data-chart-options-value="<%= @today_summary_chart[:options].to_json %>">
            <canvas class="w-full" style="height: 300px;"></canvas>
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-3 text-sm text-base-content/70">
            🌟 今日の各項目の値をドーナツ形式で表示
          </div>
        </div>
      </div>

      <!-- レベル分布チャート -->
      <div class="lg:col-span-2 neuro-card p-6">
        <h2 class="flex items-center gap-2 text-xl font-bold text-primary mb-6">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z"></path>
          </svg>
          各項目のレベル分布
        </h2>
        
        <div class="neuro-input p-6 w-full">
          <div data-controller="chart" 
               data-chart-type-value="<%= @distribution_chart[:type] %>"
               data-chart-data-value="<%= @distribution_chart[:data].to_json %>"
               data-chart-options-value="<%= @distribution_chart[:options].to_json %>">
            <canvas class="w-full" style="height: 400px;"></canvas>
          </div>
        </div>
        
        <div class="mt-4">
          <div class="neuro-badge p-4 text-sm text-base-content/70">
            📊 レベル1〜5の出現頻度を確認。どの状態が多いか把握できます。
          </div>
        </div>
      </div>

    </div>

    <!-- フッターヒント -->
    <div class="mt-8 neuro-card p-6">
      <div class="text-center">
        <h3 class="text-lg font-bold text-primary mb-4">🌱 統計活用のヒント</h3>
        <div class="grid md:grid-cols-3 gap-4 text-sm text-base-content/70">
          <div class="neuro-badge p-4">
            <strong>継続記録</strong><br>
            毎日記録することで、より正確な傾向が見えてきます
          </div>
          <div class="neuro-badge p-4">
            <strong>パターン発見</strong><br>
            曜日や時期による気分の変化を観察してみましょう
          </div>
          <div class="neuro-badge p-4">
            <strong>目標設定</strong><br>
            統計を参考に、より良い習慣づくりの目標を立てましょう
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
